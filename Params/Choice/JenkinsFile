pipeline{
   agent any
   parameters{
   choice(choices:["Dev","stag","prod"],name : "choices", description:"select env")
   }
   stages{
       stage("Demo"){
           steps{
               echo " env is ${params.choices}"
           }
       }
   }
}
