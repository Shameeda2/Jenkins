parameters{
   agent any
   parameters{
   choice(choices:["Dev","stag","prod"],defaultValue : "Dev", description:"select env")
   }
   stages{
       stage{
           steps{
               echo " env is ${params.choices}"
           }
       }
   }
}
